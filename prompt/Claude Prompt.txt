请以“一个......”的格式，生成100个日常生活用品的英文短句（不超过20个单词，不少于5个单词），注意需要使用合理的英文量词，可以添加适当的形容词（不止一个），短句之间仅用换行符分隔。另外，在每个短句的结尾使用加号（“+”）连接该短句中的核心英文单词（即这里的生活用品的单词）

请以“一只......”的格式，生成100个动物名称的英文短句（不超过20个单词，不少于5个单词），注意需要使用合理的英文量词，可以添加适当的形容词（不止一个），短句之间仅用换行符分隔。另外，在每个短句的结尾使用加号（“+”）连接该短句中的核心英文单词（即这里的动物名称单词）

请以“一辆......”的格式，生成50个交通工具的英文短句（不超过20个单词，不少于5个单词），注意需要使用合理的英文量词，可以添加适当的形容词（不止一个），短句之间仅用换行符分隔。另外，在每个短句的结尾使用加号（“+”）连接该短句中的核心英文单词（即这里的生活用品的单词）